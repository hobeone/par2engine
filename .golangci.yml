# .golangci.yml
linters:
  enable:
    - goimports
    - govet
    - staticcheck
    - errcheck
    - gosimple
    - ineffassign
    - unused

linters-settings:
  goimports:
    local-prefixes: github.com/hobeone/par2engine

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
